% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_reci_analysis.R
\name{af_create_reci_table}
\alias{af_create_reci_table}
\title{Create RECI Comparison Table}
\usage{
af_create_reci_table(
  reci_data,
  wave_transition,
  dimensions = NULL,
  use_standardized = TRUE
)
}
\arguments{
\item{reci_data}{Data frame containing RECI values as produced by af_prepare_reci_data.}

\item{wave_transition}{Character. The specific wave transition to analyze (e.g., "First-Second").}

\item{dimensions}{Character vector of dimensions to include. Default is NULL (all dimensions).}

\item{use_standardized}{Logical. Whether to include standardized RECI-S values. Default is TRUE.}
}
\value{
A data frame formatted as a RECI comparison table.
}
\description{
Creates a formatted table showing Extremism Level (EL) changes, Normative Point (NP) changes,
RECI values, and interpretation patterns for each community-dimension combination.
}
\details{
This function creates a table with the following format for each cell:
\itemize{
\item EL: <el_change>\%
\item NP: <np_change>\%
\item RECI: <reci_value> (reci-s value)
\item \if{html}{\out{
<pattern>
}}

}

The pattern is the categorical interpretation of the RECI value.
}
\examples{
# Assuming 'reci_data' is a data frame with RECI values
table <- af_create_reci_table(reci_data, wave_transition = "First-Second")

}
