% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_anova.R
\name{af_analyze_extremism_waves}
\alias{af_analyze_extremism_waves}
\title{Analyze Extremism Changes Across Wave Pairs and Population Groups}
\usage{
af_analyze_extremism_waves(
  df,
  extremism_var,
  group_var = NULL,
  wave_var = "Wave",
  respondent_id_var = "respondent_id",
  control_vars = NULL,
  panel_pairs = c(3, 4),
  alpha = 0.05,
  plot_title = NULL,
  event_names = NULL,
  line_width = 0.8,
  point_size = 3,
  plots_per_row = 3,
  use_bw = FALSE,
  color_palette = "viridis",
  show_sig = FALSE
)
}
\arguments{
\item{df}{A data frame containing the survey data with respondent_id column}

\item{extremism_var}{Character string. Name of the extremism variable}

\item{group_var}{Character string. Name of the population group variable (3 levels expected). If NULL, analyzes overall change without groups.}

\item{wave_var}{Character string. Name of the wave variable (default: "Wave")}

\item{respondent_id_var}{Character string. Name of respondent ID variable (default: "respondent_id")}

\item{control_vars}{Character vector. Names of additional categorical control variables to include as factors (default: NULL)}

\item{panel_pairs}{Numeric vector. Which pairs are panel waves (default: c(3, 4))}

\item{alpha}{Numeric. Significance level for statistical tests (default: 0.05)}

\item{plot_title}{Character string. Title for the plot (default: auto-generated)}

\item{event_names}{Character vector. Names of events for each pair (default: NULL)}

\item{line_width}{Numeric. Width of lines in plot (default: 0.8)}

\item{point_size}{Numeric. Size of points in plot (default: 3)}

\item{plots_per_row}{Numeric. Number of plots per row in facet wrap (default: 3)}

\item{use_bw}{Logical. Use black and white plot with different line types (default: FALSE)}

\item{color_palette}{Character. Color palette to use: "viridis", "default" (default: "viridis")}

\item{show_sig}{Logical. Add significance indicators to plot (default: FALSE)}
}
\value{
A list containing ANOVA results, descriptive statistics, and a ggplot object
}
\description{
This function performs mixed-model factorial ANOVA to compare extremism levels
between consecutive survey waves across different population groups. For panel
waves (pairs 3 & 4), it uses repeated measures ANOVA with matched respondent IDs.
Additional categorical control variables (like gender, age_group) can be included as factors.
}
\examples{
# Basic usage
# results <- af_analyze_extremism_waves(df, "extremism_score", "population_group")

# With categorical control variables
# results <- af_analyze_extremism_waves(df, "extremism_score", "population_group",
#                                       control_vars = c("gender", "age_group"))
# 
# With custom plot parameters
# results <- af_analyze_extremism_waves(df, "extremism_score", "population_group",
#                                       event_names = c("Event1", "Event2", "Event3", "Event4", "Event5"),
#                                       plots_per_row = 5, use_bw = TRUE, line_width = 0.6)
# 
# Overall change without groups
# results <- af_analyze_extremism_waves(df, "extremism_score", group_var = NULL)

}
