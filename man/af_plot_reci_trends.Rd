% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_reci_analysis.R
\name{af_plot_reci_trends}
\alias{af_plot_reci_trends}
\title{Create RECI Trend Visualization}
\usage{
af_plot_reci_trends(
  reci_data,
  communities = NULL,
  use_standardized = TRUE,
  separate_plots = TRUE
)
}
\arguments{
\item{reci_data}{Data frame containing RECI values as produced by af_calculate_community_reci.}

\item{communities}{Character vector of community names to include. If NULL, all communities will be included.}

\item{use_standardized}{Logical. Whether to use standardized RECI-S values. Default is TRUE.}

\item{separate_plots}{Logical. Whether to create separate plots for each community. Default is TRUE.}
}
\value{
A list of ggplot2 objects containing the RECI trend plots.
}
\description{
Creates a line plot showing RECI trends across wave transitions for different
dimensions and communities.
}
\details{
This function creates line plots showing RECI or RECI-S trends across wave transitions,
with separate lines for each dimension. It can either create separate plots for each
community or combine all communities into a single plot.

The function expects a data frame with columns:
\itemize{
\item community: The name of the community
\item dimension: The extremism dimension (cognitive, behavioral, social, overall)
\item wave_transition: The transition between waves (e.g., "First-Second")
\item reci: The RECI value
\item reci_s: The standardized RECI-S value
}
}
\examples{
# Assuming 'reci_results' is a data frame with RECI values
plots <- af_plot_reci_trends(reci_results)

# To combine all communities into a single plot
combined_plot <- af_plot_reci_trends(reci_results, separate_plots = FALSE)

}
