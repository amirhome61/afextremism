% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_reci_analysis.R
\name{af_prepare_reci_data}
\alias{af_prepare_reci_data}
\title{Prepare Data for RECI Analysis Across Waves}
\usage{
af_prepare_reci_data(
  data,
  wave_column = "Wave",
  community_variable,
  communities = NULL,
  dimensions = c("c", "b", "s", "o")
)
}
\arguments{
\item{data}{Data frame containing extremism indices from multiple waves.}

\item{wave_column}{Character. The name of the column containing wave identifiers. Default is "Wave".}

\item{community_variable}{Character. Name of the column identifying communities.}

\item{communities}{Character vector of community names to analyze. If NULL, all communities will be analyzed.}

\item{dimensions}{Character vector of dimension prefixes to analyze. Default is c("c", "b", "s", "o").}
}
\value{
A data frame containing RECI values for each community-dimension-wave transition combination.
}
\description{
Processes a data frame containing extremism indices from multiple waves
and prepares it for RECI analysis by creating wave transition pairs.
}
\details{
This function takes a data frame with extremism indices from multiple waves,
identifies consecutive wave pairs, and calculates RECI values for each transition.
It organizes the results into a format suitable for visualization with the
af_plot_reci_heatmap and af_plot_reci_trends functions.
}
\examples{
# Assuming 'indices_data' is a data frame with extremism indices from multiple waves
reci_data <- af_prepare_reci_data(indices_data, community_variable = "community")

}
