% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_manova.R
\name{af_manova_model}
\alias{af_manova_model}
\title{Create MANOVA Model for Extremism Data}
\usage{
af_manova_model(
  data,
  extremism_vars,
  wave_var,
  wave_pair,
  group_var,
  control_vars = NULL,
  id_var = NULL,
  event_var = "event_occurred",
  is_panel = FALSE
)
}
\arguments{
\item{data}{A data.frame containing the survey data}

\item{extremism_vars}{Named character vector of extremism dimension variables with display labels as names}

\item{wave_var}{Character string specifying the wave identifier variable name}

\item{wave_pair}{Numeric vector of length 2 specifying which waves to compare (e.g., c(1, 3))}

\item{group_var}{Character string specifying the grouping variable name (e.g., political orientation)}

\item{control_vars}{Character vector of control variable names}

\item{id_var}{Character string specifying the respondent ID variable name}

\item{event_var}{Character string specifying the name for the created event variable}

\item{is_panel}{Logical indicating whether the paired waves represent panel data}
}
\value{
A list containing the fitted MANOVA model, processed data, and metadata
}
\description{
Performs data preparation and fits a multivariate analysis of variance (MANOVA) model
on extremism data with wave selection and automatic event variable creation.
}
\details{
This function selects two waves from the data, creates an event variable (0 for first wave,
1 for second wave) with the name specified in event_var, and fits a MANOVA model.
The is_panel parameter indicates whether the analysis involves panel data (same individuals
across waves) or cross-sectional comparisons.
}
\examples{
\dontrun{
# Panel analysis
model <- af_manova_model(
  data = survey_data,
  extremism_vars = c("Religious" = "pe_religious", 
                     "Nationalist" = "pe_nationalist", 
                     "Political" = "pe_political"),
  wave_var = "wave",
  wave_pair = c(2, 4),
  group_var = "political_orientation",
  event_var = "pre_post_event",
  is_panel = TRUE
)

# Cross-sectional analysis
model <- af_manova_model(
  data = survey_data,
  extremism_vars = c("Ideology" = "pe_ideology", 
                     "Violence" = "pe_violence"),
  wave_var = "survey_wave",
  wave_pair = c(1, 3),
  group_var = "pol_orient",
  control_vars = c("gender", "age_group"),
  event_var = "treatment_exposure",
  is_panel = FALSE
)
}

}
