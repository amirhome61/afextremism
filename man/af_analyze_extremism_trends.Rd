% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_reci_analysis.R
\name{af_analyze_extremism_trends}
\alias{af_analyze_extremism_trends}
\title{Analyze Extremism Trends Using RECI}
\usage{
af_analyze_extremism_trends(
  data,
  wave_column = "Wave",
  community_variable,
  communities = NULL,
  dimensions = c("c", "b", "s", "o"),
  use_standardized = TRUE,
  output_dir = NULL
)
}
\arguments{
\item{data}{Data frame containing extremism indices from multiple waves.}

\item{wave_column}{Character. The name of the column containing wave identifiers. Default is "Wave".}

\item{community_variable}{Character. Name of the column identifying communities.}

\item{communities}{Character vector of community names to analyze. If NULL, all communities will be analyzed.}

\item{dimensions}{Character vector of dimension prefixes to analyze. Default is c("c", "b", "s", "o").}

\item{use_standardized}{Logical. Whether to use standardized RECI-S values. Default is TRUE.}

\item{output_dir}{Character. Directory to save visualizations. If NULL, visualizations are returned but not saved.}
}
\value{
A list containing:
\item{reci_data}{Data frame with calculated RECI values}
\item{heatmaps}{List of RECI heatmap plots}
\item{trend_plots}{List of RECI trend plots}
\item{summary_stats}{Summary statistics of RECI values}
}
\description{
Performs a comprehensive RECI analysis on extremism indices data,
calculating RECI values and generating visualizations.
}
\details{
This function performs a complete RECI analysis workflow:
\enumerate{
\item Prepares the data for analysis by identifying wave transitions
\item Calculates RECI values for each community-dimension-transition combination
\item Creates heatmap visualizations showing RECI patterns
\item Creates trend plots showing RECI changes over time
\item Calculates summary statistics for the RECI values
}

The function returns all results as a list and can optionally save visualizations.
}
\examples{
# Assuming 'indices_data' is a data frame with extremism indices from multiple waves
results <- af_analyze_extremism_trends(indices_data, community_variable = "community")

# To analyze specific communities and dimensions
results <- af_analyze_extremism_trends(
  indices_data,
  community_variable = "community",
  communities = c("Secular", "Religious"),
  dimensions = c("c", "o")
)

}
