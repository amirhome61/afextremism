% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_coefficient_visualization.R
\name{af_coef_and_ci_table}
\alias{af_coef_and_ci_table}
\title{Create coefficient and confidence interval table for multiple variables}
\usage{
af_coef_and_ci_table(models, cov_names, xpose = FALSE)
}
\arguments{
\item{models}{A named list of fitted model objects}

\item{cov_names}{A character vector of covariate names to extract, or a named vector
where names are coefficient names and values are display names}

\item{xpose}{Logical indicating whether to transpose the output (models as columns, variables as rows)}
}
\value{
A data frame with columns: model_name, cov_name, coef, conf1, conf2, pvalue, stars
If xpose = TRUE, the layout is transposed for easier comparison across models
}
\description{
This function creates a comprehensive table with coefficients, confidence intervals,
p-values, and significance stars for multiple covariates across multiple models.
}
\examples{
model1 <- lm(mpg ~ wt + hp, data = mtcars)
model2 <- lm(mpg ~ wt + hp + cyl, data = mtcars)
models <- list("Model1" = model1, "Model2" = model2)
af_coef_and_ci_table(models, c("wt", "hp"))
# With display names:
cov_names <- c("wt" = "Weight", "hp" = "Horsepower")
af_coef_and_ci_table(models, cov_names)

}
