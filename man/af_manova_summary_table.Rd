% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_manova.R
\name{af_manova_summary_table}
\alias{af_manova_summary_table}
\title{Create Summary Table of MANOVA Results Across Multiple Events}
\usage{
af_manova_summary_table(
  model_list,
  title = "MANOVA Results Summary",
  subtitle = NULL,
  variable_labels = NULL,
  event_labels = NULL,
  panel_model_ids = NULL,
  show_controls = TRUE,
  show_intercept = FALSE,
  test_statistics = c("Pillai"),
  round_digits = 3,
  highlight_panel_models = TRUE,
  add_footnotes = TRUE
)
}
\arguments{
\item{model_list}{Named list of manova_results objects (names should be event names)}

\item{title}{Character string for table title (optional)}

\item{subtitle}{Character string for table subtitle (optional)}

\item{variable_labels}{Named character vector for custom variable display names (optional)}

\item{event_labels}{Named character vector for custom event display names (optional)}

\item{panel_model_ids}{Character vector of model names that used panel data (same respondents across waves)}

\item{show_controls}{Logical indicating whether to include control variables in output}

\item{show_intercept}{Logical indicating whether to include the intercept term}

\item{test_statistics}{Character vector: which MANOVA test statistics to report (c("Pillai", "Wilks", "Hotelling", "Roy"))}

\item{round_digits}{Integer specifying number of decimal places for statistics}

\item{highlight_panel_models}{Logical indicating whether to highlight panel model columns}

\item{add_footnotes}{Logical indicating whether to add explanatory footnotes}
}
\value{
A gt table object containing formatted MANOVA results
}
\description{
Creates a comprehensive summary table of MANOVA results across multiple events,
showing multivariate test statistics with significance levels in a compact format using gt.
}
\details{
This function takes a named list of manova_results objects and creates a formatted
comparison table showing test statistics and significance levels. Multiple test statistics
can be displayed for each event. Panel models (same respondents across waves) can be
highlighted to distinguish from cross-sectional analyses.
}
\examples{
\dontrun{
# Assuming you have manova_results objects for each event
model_list <- list(
  "Inland Terror" = inland_terror_results,
  "Bennett Gov. Fall" = bennett_results,
  "Judicial Reform" = judicial_results,
  "Gallant Dismissal" = gallant_results,
  "Oct. 7th War" = oct7_results
)

# Create table with both Pillai and Wilks statistics
table <- af_manova_summary_table(
  model_list = model_list,
  test_statistics = c("Pillai", "Wilks"),
  panel_model_ids = c("Judicial Reform"),
  title = "MANOVA Results Across Destabilizing Events",
  subtitle = "Multivariate Tests of Event Effects on Political Extremism"
)
}

}
